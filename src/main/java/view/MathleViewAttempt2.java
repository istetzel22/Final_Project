package view;

import model.Equation;

public class MathleViewAttempt2 extends MathleView {
    public MathleViewAttempt2(String title, int screenWidth, int screenHeight, Equation equation, int attempt) {
        super(title, screenWidth, screenHeight, equation, attempt);

    }

}